{
  "name": "web-tower-defense",
  "version": "0.0.0",
  "private": true,
  "description": "Web-based tower defense game (Bloons-TD-inspired mechanics, all original placeholder assets).",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @td/client dev",
    "build": "pnpm --filter @td/client build",
    "preview": "pnpm --filter @td/client preview",
    "test": "pnpm --filter @td/core test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
