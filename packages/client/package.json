{
  "name": "@td/client",
  "version": "0.0.0",
  "private": true,
  "description": "Vite + PixiJS client. Renders the read-only simulation state; never mutates it.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@td/content": "workspace:*",
    "@td/core": "workspace:*",
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
